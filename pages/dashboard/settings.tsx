import { dashboardSettingsPage } from '@application';

const { getServerSideProps, SettingsPage } = dashboardSettingsPage;

// eslint-disable-next-line no-restricted-exports
export { SettingsPage as default, getServerSideProps };
