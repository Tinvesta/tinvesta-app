import {
  border,
  color,
  layer,
  opacity,
  shadow,
  typography,
} from '@infrastructure/styles/variables';

export const defaultTheme = {
  color,
  border,
  opacity,
  typography,
  shadow,
  zIndex: layer,
};
