import { functionImportTest } from '@utils';

import { mapTeamSizesToDropdownOptions } from './map-team-sizes-to-dropdown-options.function';

describe('mapTeamSizesToDropdownOptions function', () => {
  functionImportTest(mapTeamSizesToDropdownOptions);
});
