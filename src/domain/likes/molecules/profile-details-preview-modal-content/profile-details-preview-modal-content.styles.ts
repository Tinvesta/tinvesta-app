import styled from '@emotion/styled';

const StyledWrapper = styled.div`
  width: 500px;
  height: 100vh;

  & > div {
    border-radius: 0;
  }
`;

const S = {
  StyledWrapper,
};

export default S;
