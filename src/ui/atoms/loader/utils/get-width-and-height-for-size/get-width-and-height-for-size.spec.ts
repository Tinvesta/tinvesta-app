import { functionImportTest } from '@utils';

import { getWidthAndHeightForSize } from './get-width-and-height-for-size.function';

describe('getWidthAndHeightForSize function', () => {
  functionImportTest(getWidthAndHeightForSize);
});
