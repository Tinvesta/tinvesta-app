export enum ELocale {
  EN = 'en',
}

export enum EClientType {
  INVESTOR = 'investor',
  STARTUP = 'startup',
}

export enum EInvestorProfileType {
  CORPORATE = 'corporate',
  INSTITUTIONAL = 'institutional',
  PRIVATE = 'private',
}
