import { functionImportTest } from '@utils';

import { containEntersOrSpaces } from './contain-enters-or-spaces.function';

describe('containEntersOrSpaces function', () => {
  functionImportTest(containEntersOrSpaces);
});
