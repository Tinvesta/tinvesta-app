import { functionImportTest } from '@utils';

import { useConfirmationModal } from './use-confirmation-modal.hook';

describe('useConfirmationModal hook', () => {
  functionImportTest(useConfirmationModal);
});
