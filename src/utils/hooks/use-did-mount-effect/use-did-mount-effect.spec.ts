import { functionImportTest } from '@utils';

import { useDidMountEffect } from './use-did-mount-effect.hook';

describe('useDidMountEffect hook', () => {
  functionImportTest(useDidMountEffect);
});
