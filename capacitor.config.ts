import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tinvesta.app',
  appName: 'Tinvesta',
  webDir: 'out',
  bundledWebRuntime: false,
};

export default config;
